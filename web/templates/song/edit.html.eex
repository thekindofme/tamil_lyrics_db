<h2>Edit song</h2>

<%= render "form.html", changeset: @changeset, albums: @albums,
                        action: song_path(@conn, :update, @song) %>

<%= link "Back", to: song_path(@conn, :index) %>
